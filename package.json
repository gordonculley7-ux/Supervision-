{
  "name": "supervision-ceu-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Supervision & CEU Tracker \u2014 shared core, local data, and app editions.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "license": "UNLICENSED"
}